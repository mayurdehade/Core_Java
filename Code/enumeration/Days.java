package core.enumeration;

//Enum is class but is not expendable
//By default enum is extends Enum class
public enum Days {
	//this is constant elements of enum
	SUNDAY, MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY
}
